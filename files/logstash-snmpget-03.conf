input {
  file {
    path => "/var/log/snmpget"
    tags => [ "snmpget" ]
  }
}

filter {
  if "snmpget" in [tags] {
    grok {
      match => [ "message", "%{IP:host} %{GREEDYDATA:devicegroup} %{GREEDYDATA:description} %{GREEDYDATA:mib}::%{GREEDYDATA:oid} = %{SYNTAX}: %{GREEDYDATA:value}" ]
    }
  }
  if [syntax] == "INTEGER" {
    mutate {
      convert => { "value" => "integer" 
    }
  }
}

output {
  #elasticsearch {
  #  cluster => "cluster-03"
  #}
  stdout {
    codec => rubydebug
  }
}
